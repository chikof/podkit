### Set (create or update)
# @name Set
# @capture file-secret envvar.id = response.json.id
POST {{base.url}}/teams/{{team.id}}/projects/{{project.id}}/applications/{{application.id}}/env-vars
Authorization: Bearer {{auth.token}}
Content-Type: application/json

{
  "key": "GREETING",
  "value": "hello-from-env-var"
}

### List (values redacted)
# @name List
GET {{base.url}}/teams/{{team.id}}/projects/{{project.id}}/applications/{{application.id}}/env-vars
Authorization: Bearer {{auth.token}}

### Delete
# @name Delete
DELETE {{base.url}}/teams/{{team.id}}/projects/{{project.id}}/applications/{{application.id}}/env-vars/{{envvar.id}}
Authorization: Bearer {{auth.token}}
